munch.catalyst {
  consumer {
    url = "http://localhost:3333"
  }
}

munch.struct {
  document {
    url = "jdbc:postgresql://localhost:5444/postgres"
    username = "postgres"
    password = "K851xsSffyTJP3e1M7rd"

    maxPoolSize = 5
  }

  search {
    hostname = "localhost"

    port = "9200"
    scheme = "http"
  }
}