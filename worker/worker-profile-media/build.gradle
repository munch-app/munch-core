dependencies {
    compile group: 'dev.fuxing', name: 'transport-client', version: transportVersion
}

apply from: (project.rootDir.toString() + '/worker/worker.gradle')
mainClassName = 'app.munch.worker.MediaWorker'
