dependencies {
    compile project(':image')
    compile project(':services:services-instagram')
}

apply from: (project.rootDir.toString() + '/worker/worker.gradle')
mainClassName = 'app.munch.worker.InstagramWorker'
