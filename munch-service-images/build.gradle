dependencies {
    compile rootProject
    compile project(':restful-server')
    compile group: 'com.munch', name: 'munch-block-aws', version: '1.0.3'

    // Injection
    compile group: 'com.google.inject', name: 'guice', version: '4.1.0'
}

mainClassName = 'munch.images.ImageApi'