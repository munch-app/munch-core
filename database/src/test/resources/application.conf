database {
  postgres {
    url = "jdbc:postgresql://localhost:30431/postgres"
    username = "postgres_user"
    password = "postgres_passw00d"

    maxPoolSize = 10
    autoCreate = true
    waitFor = true
  }
}
