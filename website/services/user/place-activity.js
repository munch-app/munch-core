const KEY = 'user-place-activity'

import store from 'store/dist/store.modern'

function UserPlaceActivity() {


  /*
  struct UserPlaceActivity: Codable {
    var placeId: String

    var startedMillis: Int
    var endedMillis: Int?

    var actions: [Action]

    struct Action: Codable {
        var name: String
        var millis: Int
    }
}
   */
}

export default {

  /**
   *
   * @param placeId
   */
  start(placeId) {
    // Start a new session return session id



    // Push all that is still open after 30 minutes,
    // multiple tab problem cannot close immediately
  },

  /**
   *
   * @param sessionId
   */
  push(sessionId, {}) {

  }
}


